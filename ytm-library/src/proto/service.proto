syntax = "proto3";

package metadata;

service MetadataService {
  rpc LoadLibrary (LoadLibraryRequest) returns (LoadLibraryResponse) {}
}

message LoadLibraryRequest {
  string path = 1;
}

message LoadLibraryResponse {
  string message = 1;
}
